# CLAUDE.md — pointer

The project anchor moved to **[docs/CLAUDE.md](docs/CLAUDE.md)** (unedited; `git log --follow` shows it
was written before the runs it governs). This file exists only so the anchor still auto-loads into the
Claude Code session.

@docs/CLAUDE.md
